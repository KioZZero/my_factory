#ifndef PARSER_HPP
    #define PARSER_HPP

#include "Utils.hpp"

namespace Factory::Parser
{
    class Parser
    {
        public:
            Parser() = default;
            ~Parser() = default;

            int init(void);
    };
} // Factory::Parser

#endif // PARSER_HPP